'use server'
 
import Link from 'next/link'
import { redirect } from 'next/navigation'
 
export async function navigate(data: FormData) {
//   redirect(`/After`)
    // <Link href={'./After'}/>
    //   redirect(`/After/${data.get('id')}`)
}